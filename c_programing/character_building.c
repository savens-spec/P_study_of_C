/*program 2.15 characters and number*/
#include <stdio.h>

int main(void)
{
	char first = 'T';
	char second = 20;

	printf("\nThe first example of letter looks like this - %c", first);
	printf("\nThe first example of number looks like this - %d", first);
	printf("\nThe second example of letter looks like this - %c", second);
	printf("\nThe second example of number looks like this - %d", second);
	return 0;
}
