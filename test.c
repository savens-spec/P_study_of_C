#include <stdio.h>

{
	int idx1, idx2;
	for (idx1 = 0; idx1 < num2; idx1++)
	{
		for (Idx2 = 0; idx2 < num2; idx2++)
		{
			printf('#');
		}
		printf('\n');
	}
}

int main(void)
{
	printf(4, 3);
	return (0);
}
